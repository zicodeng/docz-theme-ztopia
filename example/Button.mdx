import { Props, Playground } from 'docz';
import { ThemeConsumer } from '../dist/ThemeContext';

import Button from './Button';

# Button

## Demo

<Playground>
  <ThemeConsumer.Consumer>
    {({ theme }) => (
      <React.Fragment>
        <Button theme={theme}>BUTTON</Button>
        <Button
          responsive
          theme={theme}
          style={{
            marginTop: 20,
          }}
        >
          BUTTON
        </Button>
      </React.Fragment>
    )}
  </ThemeConsumer.Consumer>
</Playground>

## API

<Props of={Button} />
