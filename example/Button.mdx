---
name: Button
menu: Components
route: /components/button
---

import { Props, Playground } from 'docz';

import { ThemeConsumer } from '../dist/helpers';

import Button from './Button';

# Button

## Demo

<Playground>
  <ThemeConsumer>
    {({ theme }) => (
      <React.Fragment>
        <Button theme={theme}>BUTTON</Button>
        <Button
          responsive
          theme={theme}
          style={{
            marginTop: 20,
          }}
        >
          BUTTON (TURN ON IFRAME MODE TO TEST)
        </Button>
      </React.Fragment>
    )}
  </ThemeConsumer>
</Playground>

## API

<Props of={Button} />
